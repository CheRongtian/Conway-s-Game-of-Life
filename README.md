# LifeLab

## Build

```sh
mkdir build
cd build
cmake ..
make
```

## Run

```sh
./build/LifeLab
```

Controls:

- `Space`: pause or resume
- `N`: advance one generation while paused
- `R`: reset the current collision
- `Esc`: quit
